syntax = "proto3";

package egg;
option go_package="./egg";

message EggRequest {
  string content = 1;
}

message EggResponse {
  string comment = 1;
}

service Egg {
  rpc Comment(EggRequest) returns(EggResponse);
}
